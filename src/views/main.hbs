{{#if logged}}
    {{#if test}}
        <div class="container container-layout my-5">
            {{>productos}}
        </div>
    {{else}}
        <div class="container container-layout my-5">
            {{>chat}}
        </div>
    {{/if}}
{{else}}
    <div class="container container-layout my-5">
        {{>logout}}
    </div>
{{/if}}